{
  "name": "home-coffing",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:deploy": "git pull && docker-compose pull && docker-compose up -d",
    "docker:deploy:local": "git pull && docker-compose build && docker-compose up -d",
    "schema:create": "pnpm --filter @home-coffing/api schema:create",
    "schema:update": "pnpm --filter @home-coffing/api schema:update",
    "schema:drop": "pnpm --filter @home-coffing/api schema:drop",
    "db:reset": "pnpm schema:drop && pnpm schema:create"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core"
    ]
  },
  "packageManager": "pnpm@10.8.1"
}
