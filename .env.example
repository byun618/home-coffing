DB_HOST=localhost
DB_PORT=3306
DB_USER=home_coffing
DB_PASSWORD=
DB_NAME=home_coffing

# Auth (필수 — 임의 문자열 32자 이상 권장)
JWT_ACCESS_SECRET=change-me-access-secret-32chars-min
JWT_REFRESH_SECRET=change-me-refresh-secret-32chars-min
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

# 외부 서비스 (Phase 1은 stub, 비워두면 NoOp)
# AMPLITUDE_API_KEY=
# FCM_SERVICE_ACCOUNT_JSON=
